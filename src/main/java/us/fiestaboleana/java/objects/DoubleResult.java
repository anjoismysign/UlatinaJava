package us.fiestaboleana.java.objects;

/**
 * @param value   value of the result, in this case a double
 * @param isValid true if the result is valid, false otherwise
 * @author anjoismysign
 */
public record DoubleResult(double value, boolean isValid) {
}
