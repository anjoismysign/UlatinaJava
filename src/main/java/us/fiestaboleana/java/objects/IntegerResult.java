package us.fiestaboleana.java.objects;

/**
 * @param value   value of the result, in this case an integer
 * @param isValid true if the result is valid, false otherwise
 * @author anjoismysign
 */
public record IntegerResult(int value, boolean isValid) {
}
